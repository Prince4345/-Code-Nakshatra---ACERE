services:
  - type: web
    name: carbontrace-gee-backend
    runtime: docker
    rootDir: backend
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 5000
      - key: FRONTEND_ORIGINS
        sync: false
      - key: GEE_SERVICE_ACCOUNT_PATH
        sync: false
